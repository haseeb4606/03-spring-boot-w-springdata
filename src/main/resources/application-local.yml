server:
   port:9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_db
    username: root
    password: Root   
     
  jpa:
    properties:
      hibernate:
        show_sql: false
        format_sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
